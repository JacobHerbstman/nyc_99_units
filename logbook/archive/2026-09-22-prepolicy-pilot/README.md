# Pilot before the pre-policy source correction

These saved pilot results use the 554-parent historical and 310-parent post-policy weighting samples preceding the September 22 source correction. They are dated evidence for the logbook, not estimates from the corrected 23Q4 historical panel. `input_provenance.csv` records the original input fingerprints; `analysis_parents.csv` preserves the fitted sample. Files were copied without rewriting their bytes.

The root logbook build reads this snapshot. Model estimation remains an explicit `make pure-notch-pilot` operation while the boundary review continues.
