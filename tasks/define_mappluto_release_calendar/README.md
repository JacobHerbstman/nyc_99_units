# Define MapPLUTO Release Calendar

Creates the release calendar used for as-of HDB-to-MapPLUTO matching.

The calendar is task-local source metadata. `safe_available_date` is the conservative date after which a release can be used for pre-filing matching. For archived releases, it is based on the maximum internal file timestamp in the official DCP archive zip.
