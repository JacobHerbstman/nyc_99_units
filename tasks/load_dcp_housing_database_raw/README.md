# Load DCP Housing Database Raw

Loads the zipped DCP Housing Database project-level CSV into a normalized raw parquet file.

This task preserves the raw fields after simple name normalization and source metadata attachment. Cleaning decisions belong in downstream staging or audit tasks.
