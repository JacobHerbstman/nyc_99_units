# Fetch ACRIS DEED Master

Fetches standard ACRIS Real Property Master `DEED` documents for 2010-2025, plus the official ACRIS document control-code table. This is the first conservative ACRIS recovery layer for the opportunity-site sales panel.

The task only writes a file manifest. Raw CSV partitions stay in `data_raw/`.
