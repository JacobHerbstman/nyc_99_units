# Fetch ACRIS Opportunity Records

Fetches ACRIS Real Property Legals rows for candidate ACRIS `DEED` documents linked to opportunity-block DOF sale seeds by exact borough/date/amount.

The task writes a raw-file manifest only. Raw CSV chunks stay under `data_raw/dof_acris_real_property_legals/`.
