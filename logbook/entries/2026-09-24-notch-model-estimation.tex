\clearpage
\section*{September 24, 2026: Estimating the size-and-splitting model from scratch}
\textit{Agent-drafted research entry.}

The September pilot reproduced the spike at 99 but not how parents split. It
predicted 4.4 percent of recent parents with two filings against 13.2
observed, 12.3 percent with three or more against 3.6, and almost no $99+99$
parents. Every alternative filing count drew its own cost of the same size, so a
parent that split spread across two to six filings. Jacob asked for a
splitting cost that rises with the number of buildings, so that pairs and
$99+99$ appear, and for a new estimation replacing
\path{fit_pure_notch_pilot}. It lives in the production task
\path{tasks/estimate_notch_model}.

\paragraph{The model.} It follows \path{framework_writeup.tex}. Each weighted
historical parent brings its preferred total $x$ and building count $J^0$.
Under the policy it chooses total units $m$ and buildings $J$ to minimize,
relative to its ordinary cost of 99 units, the size loss $((m-x)/99)^2$, a
burden on each building with 100 or more units (a jump $\kappa$ plus a kink
$\tau$ that raises the cost of each unit above 100), and a cost $c$ for each
building added beyond $J^0$. Parents differ in $c$, which is exponential with
mean $\sigma$. The historical organization is free, so the ordinary
multi-building parents of 2019--2022 stay as they were unless the threshold
gives them a reason to change. Layouts are free, sizes are exact integers, and
choice probabilities are computed exactly. The parent's own land does not
enter: consistent with the September 24 weights decision, zoning and borough
are fixed traits of the opportunity, and the size loss includes any change in
land. The weights match residential FAR and borough. The objective is the
sum of squared differences over the pilot's 45 cells (one, two or three-plus
buildings by 15 size bins), searched on a grid.

Before fitting, the task checks that without the burden every parent keeps
$x$ and $J^0$; that a single building shrinks to 99 exactly when
$x\le99(1+\sqrt\kappa)$ and is compressed by $1+\tau$ above; that minimum
burdens match exhaustive partitions for two and three buildings; that the
building menu never binds; that no parent consolidates; and that the exact
probabilities match simulated cost draws.

\paragraph{Estimates.} The jump is $\kappa=0.16$: crossing 100 costs a
single-building parent 16 percent of the ordinary cost of 99 units, so it
shrinks to 99 when its preferred size is below about 138. The mean splitting
cost is $\sigma=0.20$ per added building. The data choose no kink ($\tau=0$),
so the pure notch gives the same fit. Points within 10 percent of the best
objective span $\kappa$ from 0.12 to 0.30, $\tau$ from 0 to 0.02 and $\sigma$
from 0.16 to 0.50.

\begin{center}
\small
\begin{tabular}{lrrrr}
\hline
Share of recent parents (\%) & Observed & Benchmark & Model & Pilot \\
\hline
Exactly 99 units & 11.8 & 0.6 & 11.0 & 12.6 \\
Two buildings & 16.4 & 9.3 & 18.3 & 4.4 \\
Organized as $99+99$ & 3.0 & 0.0 & 1.4 & 0.2 \\
Three or more buildings & 4.3 & 3.3 & 8.6 & 12.3 \\
Above 300 units & 8.6 & 14.6 & 14.2 & 9.8 \\
Mean units & 139.5 & 170.9 & 169.0 & 149.9 \\
\hline
\end{tabular}
\end{center}

The benchmark is the reweighted 2019--2022 distribution. The pilot column is
the archived September 22 fit on the earlier panel, whose observed shares
differ (13.2 percent pairs, 2.9 percent $99+99$).

The rising cost does what was intended. Pairs are now slightly
overpredicted, and the model produces half the observed $99+99$ parents,
against almost none before. Three-plus parents are still twice the observed
share, mostly above 300 units: when splitting is nearly free, a large
single-building parent keeps its size in as many buildings as it takes. The
exponential puts much of its mass near zero cost, so a distribution with less
mass there is the natural next specification.

\paragraph{Why no kink.} The best fit worsens steadily as the kink grows: the
objective is 0.0060 with no kink, 0.0064 at $\tau=0.02$, 0.0079 at 0.1 and
0.0092 at 0.2, each with $\kappa$ and $\sigma$ re-optimized. A kink raises the
cost of every unit above 100 in a building, so it gives every crossing parent
a reason to split. To keep splitting from exceeding what is observed, the fit
must then raise the splitting cost (from 0.20 to 1.6 at $\tau=0.1$). That
removes the pairs of 120--197 units (0.8 percent of parents at 120--149 in two
buildings, against 3.6 observed) and sends those parents to 99 instead (14.6
percent against 11.8). The kink does help elsewhere: it shrinks large single
buildings toward the observed tail (4.3 percent above 300 against 5.6
observed, 8.0 without a kink) and leaves more single buildings at 100--149.
The pairs and the spike at 99 outweigh those gains. The preference matters for
units: at $\tau=0.1$ the model loses 3,024 units rather than 596, so the kink
is the channel through which the model could explain the tail, and the data
reject it only because it conflicts with cheap splitting.

\paragraph{Units.} Scaled to the 304 recent parents, the model implies 596
fewer proposed units. Holding each parent's building count fixed would lose
779, so splitting preserves 183. The raw difference between the weighted
historical total and the recent total is 9,555 units. Parents above 300 units
account for 10,743, more than all of it, with smaller sizes offsetting; the
model changes little there. The data reject compressing those
projects with a kink; the recent period simply has fewer large parents. With
lot-area weights the raw difference falls to 2,084 units and the model
explains 659 of them. Whether the missing large projects are a response to the
policy, such as assembling less land or not filing, or a change in available
sites is not something this model can settle.

\begin{center}
\small
\begin{tabular}{lrrrrr}
\hline
Specification & $\kappa$ & $\tau$ & $\sigma$ & Objective & Units lost \\
\hline
Main (jump and kink) & 0.16 & 0 & 0.20 & 0.0060 & 596 \\
Splitting cost $c, 3c, 6c$ & 0.16 & 0.02 & 0.32 & 0.0057 & 1,205 \\
Lot-area weights & 0.18 & 0 & 0.25 & 0.0024 & 659 \\
Common 180-day horizon & 0.16 & 0 & 0.16 & 0.0050 & 342 \\
Curvature 0.5 & 0.06 & 0 & 0.08 & 0.0059 & 689 \\
Curvature 2 & 0.60 & 0 & 0.79 & 0.0062 & 581 \\
Joint assessment & 0.03 & 0.20 & -- & 0.0099 & 6,950 \\
\hline
\end{tabular}
\end{center}

The jump and splitting cost are stable across weights and the common
follow-up horizon (187 recent parents). Curvature changes their scale, as it
should, not the fit. A splitting cost that rises faster per building fits
slightly better and doubles the units lost, because more parents shrink to
99 or $99+99$ (12.2 and 1.9 percent) instead of adding buildings; the unit
estimate depends on this shape.
Joint assessment of the whole parent fits much worse and produces no $99+99$
parents; splitting cannot help, so $\sigma$ is not identified.

\paragraph{Can the data tell the parameters apart?} Redrawing 200 recent
samples from the model at the estimate and re-estimating each, the jump falls
between 0.12 and 0.22 in 80 percent of draws, the kink is estimated at zero in
89 percent, and $\sigma$ lies between 0.13 and 0.32. If the true kink were 0.2,
its estimates would range from 0.05 to 0.6, with the jump anywhere from 0.02 to
1: a kink is weakly identified. These draws hold the historical sample fixed,
so they understate uncertainty.

\paragraph{Not yet done.} A splitting-cost distribution with less mass near
zero; inference that also resamples historical parents and weights; and the
legal-assessment question for observed splits. Jacob decided not to model the
150-unit regime in Zones A and B for now.

\textit{Reproduction note.} Root \texttt{make estimate} runs
\path{tasks/estimate_notch_model}; results are in \path{estimates.csv},
\path{fit_moments.csv} and \path{recovery_summary.csv}.

\subsection*{September 24 follow-up: parents up to 300 units and a steeper splitting cost}

Jacob decided to set aside parents above 300 units. They dominate the unit
totals, and the before and after counts of very large projects cannot be
distinguished from a change in which projects reach filing. The main fit now
compares the size-and-building distribution of parents with at most 300 units,
renormalized on both sides, so the number of very large projects in either
period does not enter; a historical parent just above 300 that shrinks into the
range still counts. Units lost are counted among historical parents of at most
300 units, scaled to the 278 recent parents in that range. Second, the cost of
adding $k$ buildings is now $c\,k^\gamma$ with $\gamma$ estimated: $\gamma=1$
is the linear cost of the first fit, and $\gamma>1$ makes each added building
cost more than the last. Under the linear cost, a 220-unit building whose
splitting cost is small prefers three buildings at full size to $99+99$; the data
show the opposite.

\begin{center}
\small
\begin{tabular}{lrrrr}
\hline
Share of parents up to 300 units (\%) & Observed & Benchmark & Main & Linear cost \\
\hline
Exactly 99 units & 12.9 & 0.6 & 12.6 & 12.9 \\
Organized as $99+99$ & 3.2 & 0.0 & 3.1 & 2.3 \\
Two buildings & 16.2 & 7.4 & 20.6 & 17.1 \\
Three or more buildings & 3.2 & 1.9 & 4.8 & 6.0 \\
Mean units & 107.3 & 110.4 & 109.7 & 109.9 \\
\hline
\end{tabular}
\end{center}

The main estimates are a jump $\kappa=0.10$, a kink $\tau=0.05$, growth
$\gamma=1.75$ and a mean splitting cost $\sigma=0.25$. The model now matches the
99 spike and the $99+99$ parents almost exactly, and three-plus parents fall to
4.8 percent against 3.2 observed. The steeper cost is favored but loosely: the
objective is 0.0029 at $\gamma=1$ and 0.0026 from 1.5 to 2, and every value from
1 to 3 is within 10 percent of the best. In simulated samples from the estimate,
$\gamma$ is re-estimated between 1.25 and 2.5 in 80 percent of draws; from a
linear truth it is re-estimated at 1 in 69 percent.

Two buildings are now overpredicted, 20.6 percent against 16.2. About a third of
that is ordinary two-building parents of 50--89 units in the benchmark (3.1
percent against 1.8 observed), which the policy does not move; the rest is
pairs of 150--249 units. The model also cannot place the 3.2 percent of recent
single buildings at 95--98 units, just short of 99.

Without the largest projects the kink returns. The best objective is 0.0033
with no kink, 0.0026 at $\tau=0.05$ and 0.0030 at 0.1; from a truth of 0.05,
simulated estimates fall between 0.02 and 0.10. The September 24 finding of no
kink therefore came from the all-size comparison. The kink drives the unit
estimate: the model loses 636 units with no kink, 1,018 at 0.05 and 1,505 at
0.1.

\begin{center}
\includegraphics[width=\textwidth]{../tasks/estimate_notch_model/output/pdf/model_fit.pdf}
\end{center}

\paragraph{Units.} Among parents of at most 300 units, the model loses 1,018
units; holding building counts fixed it would lose 1,368, so splitting
preserves 350. The raw difference in mean size within this range, scaled to
the 278 recent parents, is 854 units, so the model accounts for all of it. The
estimates are sensitive to the weights and follow-up: with lot-area weights
the historical parents in this range are smaller than the recent ones (a raw
gap of $-908$), yet the model loses 1,373; on the common 180-day horizon it
loses 436 of a raw 393 on 167 recent parents.

\begin{center}
\small
\begin{tabular}{lrrrrrr}
\hline
Specification & $\kappa$ & $\tau$ & $\gamma$ & $\sigma$ & Objective & Units lost \\
\hline
Main (up to 300) & 0.10 & 0.05 & 1.75 & 0.25 & 0.0026 & 1,018 \\
Linear splitting cost & 0.10 & 0.05 & 1 & 0.32 & 0.0029 & 1,025 \\
Lot-area weights & 0.10 & 0.10 & 1.5 & 0.40 & 0.0018 & 1,373 \\
Common 180-day horizon & 0.12 & 0.02 & 2 & 0.16 & 0.0031 & 436 \\
Curvature 0.5 & 0.04 & 0.02 & 2 & 0.10 & 0.0025 & 1,102 \\
Curvature 2 & 0.40 & 0.05 & 1.5 & 0.79 & 0.0027 & 748 \\
Joint assessment & 0.09 & 0.10 & -- & -- & 0.0101 & 2,232 \\
All sizes & 0.16 & 0.02 & 1.75 & 0.32 & 0.0057 & 1,213 \\
\hline
\end{tabular}
\end{center}

Objectives are comparable within the up-to-300 rows, not with the all-size
row. Joint assessment again fits worst and produces no $99+99$ parents.

\paragraph{Below 250 units.} Jacob asked whether the kink survives a lower
cutoff. Comparing parents below 250 units (266 recent parents), the estimates
are the same jump and kink, $\kappa=0.10$ and $\tau=0.05$, with $\gamma=1.5$ and
$\sigma=0.25$. The case for the kink is slightly stronger: with no kink the best
objective is 0.0035, 35 percent worse than 0.0026, and points within 10 percent
of the best have $\tau$ from 0.02 to 0.10. Among historical parents below 250
units, the model loses 926 proposed units, scaled to the 266 recent parents in
that range, against a raw difference in mean size of 1,227; splitting preserves
307. The recent period covers January 1, 2025 to July 8, 2026, about 1.5 years,
so this is roughly 600 units a year among the program's plausible rental
opportunities of 50--249 units, conditional on the number of parents. The
number depends on the kink: 646 units with none, 1,207 at $\tau=0.1$.
