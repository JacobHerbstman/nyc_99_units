\clearpage
\section*{September 10, 2026: A smaller data and figures pipeline}
\textit{Agent-drafted implementation note.}

The main inventory falls from 29 tasks to 19, including environment setup. The other 18 acquire city records, construct constituent and parent data, apply documented decisions, and produce the current plots and maps. Nine former task boundaries are consolidated: loading with staging, calendar and lot-crosswalk preparation with parcel matching, adjacency and recent-filing preparation with parent construction, and supporting exposure assembly with classification. Main R code falls from 64 scripts and 10,900 lines to 46 scripts and 6,892 lines.

The older 2011--2022 distribution analysis, source-registry checks, and exploratory scale-and-shape calculations move into audits. Two focused tasks own the final panels and basic citywide plots; exploratory calculations consume these canonical products. Ordinary \texttt{make} builds the current plots and maps, while \texttt{make data} requests the panels. The framework and its existing exploratory illustrations retain an explicit build. No main task depends on an audit.

A full rebuild exactly reproduces the 2,733-row parent panel, 2,916-row constituent panel, 8,054-row membership table, and 689-row accepted-link table. Both parent-distribution CSV tables are byte-identical, and all ten individual citywide figures and three borough figures render identically. The shorter citywide pack contains only those ten basic figures. Of 122 main data reports, 117 are unchanged. Four file-index reports and the site-panel report have different fingerprints because stored paths moved; restoring only the old path strings in temporary copies reproduces all five original fingerprints.

The Make dry-run recursion is fixed by invoking companion recovery through a shared command variable; ordinary upstream checks retain direct Make calls. The actual root build succeeds, and an unchanged second build runs no R producer or LaTeX compilation. Removing a parent-distribution PDF triggers its producer once, restores the figure and reports, and leaves the next build unchanged. A disposable GNU Make 3.81 example also verifies fresh builds, changed-input propagation, and missing-companion recovery under parallel invocation.

Several audit summaries were stale relative to earlier parent corrections. Rebuilding their unchanged scripts updates the broader exposure summary from 6,721 to 6,717 parents, its review queue from 491 to 486 rows, and its unit-by-exposure table from 931 to 932 cells. The older post-99 gap audit now reports 41 exact-99 parents rather than 43. These are downstream refreshes; the current central tables are unchanged. All twenty retained scale-and-shape audit CSV tables reproduce exactly.

Eighteen of nineteen affected audit builds pass. The older condo-tenure audit stops because its saved Attorney General searches do not cover its current universe under its parent-and-filing keys. It now reads the byte-identical frozen tables used by production, without triggering a live search. Its coverage assertion remains enforced. The separate live-search acquisition task receives a dependency dry run only. Neither task feeds the main results. The remaining membership candidates, incomplete features, short post-policy follow-up, and legal wage-assessment classification discussed in the preceding entry still require research decisions before estimation.

\textit{Reproduction:} This cleanup follows commit \texttt{25cdf36} on \texttt{simplify\_empirical\_tasks}. Administrative vintages and sample periods are unchanged. The shared execution change also rebuilds fixed-2023 ACS acquisition for earlier logbook entries; all three files match their saved SHA-256 fingerprints. The task graph is generated from Make prerequisites. No joint model is estimated here.
