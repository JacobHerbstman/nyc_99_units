# Source Registry

This task tracks official source locations, manual drops, and agency-request lanes before any source-specific fetch/load/stage tasks exist.

The validation output is `tasks/source_registry/output/source_registry_checks.csv`.
