# Build ACRIS Recovered Lot-Quarter Incidence

Expands conservative ACRIS recovered sale events to frozen primary opportunity BBLs and event quarters.

This is an incidence file, not an independent per-lot price dataset. Document-level event price is carried with explicit allocation variables so downstream tasks do not accidentally duplicate document prices across multi-lot events.
